define(function (require) {
	var app = require('../app');
	var $ = require('jquery');
	require('metisMenu');

	app.controller('indexCtrl', ['$scope', function ($scope) {
		// $('#side-menu').metisMenu();

		// var service = app.get('indexService');

		// $scope.user = service.getUser();
		// console.log('user');
		// console.log($scope.user);

		console.log('11111111111111111111111');

	}]);

});